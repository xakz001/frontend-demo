apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-demo
  template:
    metadata:
      labels:
        app: frontend-demo
    spec:
      containers:
        - name: frontend
          image: aurait/frontend-demo:latest
          ports:
            - containerPort: 80
